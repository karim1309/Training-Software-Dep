#include <stdio.h>
#include <math.h>
#include <stdlib.h>
#define max 100
int main() {

    int n,arr[max],i;
    scanf("%d",&n);
    for(i=n-1;i>=0;i--)
    {
        scanf("%d",&arr[i]);
        
    }
    for(i=0;i<n;i++)
    printf("%d ",arr[i]);
    return 0;
}
